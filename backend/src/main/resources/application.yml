server:
  port: 8080

spring:
  application:
    name: ai4note-backend
  datasource:
    url: jdbc:mysql://localhost:3306/ai4note?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root

ai4note:
  storage-root: ../storage

